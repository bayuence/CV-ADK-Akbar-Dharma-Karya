[build]
  command = "pnpm build"
  publish = "out"

[build.environment]
  STATIC_EXPORT = "true"
  NODE_VERSION = "20"

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
